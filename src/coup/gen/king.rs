use crate::{coup::rep::Move, game::position::Position, notation::Square, types::{Bitboard, Direction}};

pub fn generate_moves(position: &Position) -> impl Iterator<Item = Move> {
    vec![].into_iter()
}
